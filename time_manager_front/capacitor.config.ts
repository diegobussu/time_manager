import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sync.shift.app',
  appName: 'sync.shift.app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
